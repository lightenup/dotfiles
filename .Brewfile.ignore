# Ignore list for task brew:reconcile
#
# One package/tap/extension per line, using the exact name from Brewfile.
# Examples:
# openssl@3
# homebrew/cask/font-jetbrains-mono-nerd-font
# ms-python.python

# Installed on this machine but intentionally undeclared: the pkg installer
# needs sudo, which breaks unattended `./install.sh` runs.
blackhole-2ch
