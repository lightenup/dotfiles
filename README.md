# dotfiles

Mac setup: shell, git, hooks, SSH, zsh completions, Brewfile.

## New machine

```bash
git clone git@github.com:lightenup/dotfiles.git ~/Development/private/dotfiles
cd ~/Development/private/dotfiles
./install.sh
```

Then manually copy SSH keys to `~/.ssh/`.
